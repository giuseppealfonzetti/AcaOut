% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residuals.R
\name{conditioned_residuals}
\alias{conditioned_residuals}
\title{Grade and time residuals conditioned on plug-in latent traits}
\usage{
conditioned_residuals(FIT, LATMAT = NULL, LAST_DAY = NULL)
}
\arguments{
\item{FIT}{Output from \link{fit_EM} or \link{fit_BFGS}.}

\item{LATMAT}{Optional \verb{n_obs x 2} matrix of ability and speed to plug in.
Defaults to the MAP scores from \code{\link[=compute_map]{compute_map()}}.}

\item{LAST_DAY}{Optional last observed day per student, at which the times are
truncated. Defaults to \code{FIT$data$max_time}.}
}
\value{
A list of four \verb{n_obs x n_exams} matrices: \code{grade} and \code{time} hold the
observation minus its conditional expectation, \code{grade_sd} and \code{time_sd} the
conditional standard deviations. \code{grade} and \code{time} follow the observation
pattern of the data; the standard deviations are given wherever they exist.
}
\description{
Residuals of the measurement model at a single value of the latent traits
rather than integrating them out. Each residual is the observation minus its
conditional expectation over the set where the exam was observed; numerators
and standard deviations are returned separately, so the standardised residual
is \code{grade / grade_sd} and \code{time / time_sd}. With the MAP scores as plug-in,
the cross-exam correlations of the grade residuals are Yen's Q3 statistics.
}
