% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residuals.R
\name{simulate_residuals}
\alias{simulate_residuals}
\title{Residuals of datasets simulated under local independence}
\usage{
simulate_residuals(
  FIT,
  B = 1000,
  NCORES = NULL,
  SEED = 1,
  ADMIN_YEAR = NULL,
  YEAR_OFFSET = 0L,
  REFIT = NULL,
  VERBOSE = FALSE
)
}
\arguments{
\item{FIT}{Output from \link{fit_EM} or \link{fit_BFGS}.}

\item{B}{Number of replications.}

\item{NCORES}{Number of cores. Defaults to \code{detectCores() - 1}, or \code{1} on Windows.}

\item{SEED}{Replication \code{b} is simulated with seed \code{SEED + b}.}

\item{ADMIN_YEAR}{Last year of follow up per student, passed to \code{\link[=simulate_from_fit]{simulate_from_fit()}}.}

\item{YEAR_OFFSET}{Day of the year separating two academic years.}

\item{REFIT}{Optional control list for \code{\link[ucminf:ucminf]{ucminf::ucminf()}}, passed to \code{\link[=fit_BFGS]{fit_BFGS()}}.
When given, each replication is refitted from \code{FIT$fit$par}; the convergence
codes are returned in \code{meta}.}

\item{VERBOSE}{TRUE to report progress.}
}
\value{
A list with \code{observed} (residuals of the data \code{FIT} was estimated on),
\code{simulated} (the \verb{n_obs x n_exams x B} arrays \code{grade}, \code{time}, \code{grade_sd},
\code{time_sd}) and \code{meta} (the generating arguments and, with \code{REFIT}, the
convergence code of each replication).
}
\description{
Simulates \code{B} datasets from \code{FIT} with \code{\link[=simulate_from_fit]{simulate_from_fit()}}, which satisfy
local independence by construction, re-scores the latent traits on each and
returns the residuals. Nothing is summarised: correlation panels and tests are
left to the caller. By default parameters are held at \code{FIT$fit$par} and only
the latent scores are re-estimated; supplying \code{REFIT} re-estimates the
parameters too, warm starting \code{\link[=fit_BFGS]{fit_BFGS()}} at \code{FIT$fit$par}.
}
\details{
The returned object is large: with \code{n_obs = 786}, \code{n_exams = 22} and
\code{B = 1000} the four arrays occupy about 553 MB in memory.
}
